# Changelog

The project changelog is maintained in the repository root at `CHANGELOG.md`.

For each GitHub release, notes are generated automatically from merged pull requests and commits.

See the latest entries in the root changelog file.
